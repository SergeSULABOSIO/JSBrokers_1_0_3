<div class="p-8">
    <h1 class="text-3xl font-bold mb-4">{{ title ?? 'Titre par défaut' }}</h1>
    <p class="text-gray-700">
        {{ content ?? 'Contenu par défaut du composant.' }}
    </p>
</div>