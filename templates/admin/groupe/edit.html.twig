{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	{{ "groupe_description" | trans }}
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	<div class="container">
		{# {{ form(form) }} #}
		<div class="card shadow-sm bg-body" style="width: 55rem;">
			<div class="card-body">
				<p>
					{{ "groupe_form_description" | trans({
						":user": utilisateur.nom, 
						":groupe": groupe.nom, 
					}) | raw }}
				</p>
				<div class = "m-2">
					{{ form(form, {action: path('admin.groupe.edit', {
						idEntreprise:entreprise.id,
						idGroupe:groupe.id
						})}) 
					}}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
