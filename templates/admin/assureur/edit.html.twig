{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	{{ "assureur_form_description" | trans({
		":user": utilisateur.nom, 
		":assureur": assureur.nom, 
	}) | raw }}
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	{# {{ form(form) }} #}
	{{ form_start(form, {
		'attr': {
			'data-controller': 'assureur-formulaire', 
			'data-assureur-formulaire-idassureur-value': assureur.id,
			'data-assureur-formulaire-identreprise-value': entreprise.id,
			}
		}) 
	}}
	<div class="d-flex shadow-sm card bg-body sensible bg-white">
		<div class="card-body">
			<div>
				{# Les onglets #}
				<nav>
					<div class="nav nav-tabs" id="nav-tab" role="tablist">
						{# Onglet Principale - tittre #}
						<button class="nav-link fw-bold text-secondary active" id="nav-assureur-tab" data-bs-toggle="tab" data-bs-target="#nav-assureur" type="button" role="tab" aria-controls="nav-assureur" aria-selected="true">
							<small>
								{{ include('segments/icones/assureur.html.twig', {size:'20px'}) }}
								PRINCIPAL 
							</small>
						</button>
					</div>
				</nav>
				{# Contenu de chaque Onglet #}
				<div class="tab-content" id="nav-tabContent">
					{# Onglet Principale - corps #}
					<div class="tab-pane fade m-2 show active" id="nav-assureur" role="tabpanel" aria-labelledby="nav-assureur-tab" tabindex="0">
						<div class="row">
							<div class="col">
								{{ form_row(form.nom, {'attr': {'data-assureur-formulaire-target': 'nom'}}) }}
							</div>
						</div>
						<div class="row">
							<div class="col">
								{{ form_row(form.numimpot) }}
							</div>
							<div class="col">
								{{ form_row(form.rccm) }}
							</div>
							<div class="col">
								{{ form_row(form.idnat) }}
							</div>
						</div>
						<div class="row">
							<div class="col">
								{{ form_row(form.adressePhysique) }}
							</div>
							<div class="col">
								{{ form_row(form.email) }}
							</div>
							<div class="col">
								{{ form_row(form.url) }}
							</div>
						</div>
					</div>
				</div>
				<div class="row">
					<div class="col">
						{{ form_widget(form.enregistrer, {
							'attr': {
								'data-assureur-formulaire-target': 'btEnregistrer'
								}
							}) 
						}}
					</div>
				</div>
			</div>
			<small>
				<span class="text-secondary m-2 fw-bold" data-assureur-formulaire-target="display" style="display:block;">Prêt</span>
			</small>
		</div>
	</div>
	{{ form_end(form) }}
{% endblock %}
