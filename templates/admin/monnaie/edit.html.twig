{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	En tant qu'application de gestion des finances, vous devez définir un ou plusieurs monnaies de transaction ainsi que leurs taux respectifs.
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	<div class="container">
		{# {{ form(form) }} #}
		<div class="card shadow-sm bg-body" style="width: 40rem;">
			<div class="card-body">
				<p>
					{{ "currency_form_description" | trans({
						":user": utilisateur.name, 
						":currency": monnaie.nom, 
					}) | raw }}
				</p>
				{# <turbo-frame id="monnaie_{{ monnaie.id }}"> #}
					<div class = "m-2">
						{{ form(form, {action: path('admin.monnaie.edit', {
							idEntreprise:entreprise.id,
							idMonnaie:monnaie.id
							})}) 
						}}
					</div>
				{# </turbo-frame> #}
			</div>
		</div>
	</div>
{% endblock %}
