{# Bascule de langue (FR/EN) de la barre de titre des pages « entreprise ».
   Composant unifié : on reconstruit la route courante (route + paramètres +
   query string) pour rester sur la même page — y compris l'édition et son
   paramètre `id` — en ne changeant que `lang`. ?lang= persiste la préférence
   (cf. EntrepriseController::applyLangPreference). Voir components/_lang_switch.html.twig. #}
{{ include('components/_lang_switch.html.twig', {uid: 'lang-ent'}) }}
