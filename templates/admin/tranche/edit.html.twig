{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	{{ "tranche_form_description" | trans({
		":user": utilisateur.nom, 
		":tranche": tranche.nom, 
	}) | raw }}
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	{# {{ form(form) }} #}
	{{ form_start(form) }}
	<div class="d-flex shadow-sm card bg-body sensible bg-white">
		<div class="card-body">
			<div>
				<div class="row">
					<div class="col">
						{{ form_row(form.nom) }}
					</div>
					<div class="col">
						{{ form_row(form.montantFlat) }}
					</div>
					<div class="col">
						{{ form_row(form.pourcentage) }}
					</div>
				</div>
				<div class="row">
					<div class="col">
						{{ form_row(form.payableAt) }}
					</div>
					<div class="col">
						{{ form_row(form.echeanceAt) }}
					</div>
				</div>
				<div class="row">
					<div class="col">
						{{ form_widget(form.enregistrer) }}
					</div>
				</div>
			</div>
		</div>
	</div>
	{{ form_end(form) }}
{% endblock %}
