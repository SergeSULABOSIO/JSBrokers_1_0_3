{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	{{ "feedback_description" | trans }}
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	<div class="container">
		{# {{ form(form) }} #}
		<div class="card shadow-sm bg-body" style="width: 55rem;">
			<div class="card-body">
				<p>
					{{ "feedback_form_description" | trans({
						":user": utilisateur.nom, 
						":feedback": feedback.description, 
					}) | raw }}
				</p>
				<div class = "m-2">
					{{ form(form, {action: path('admin.feedback.edit', {
						idEntreprise:entreprise.id,
						idFeedback:feedback.id
						})}) 
					}}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
