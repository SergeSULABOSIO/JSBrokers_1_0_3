{% extends 'base.html.twig' %}

{% block title %}
	{{pageName}}
{% endblock %}

{% block section_utilisateur %}
	{% include 'segments/boutons_entreprise_dashbord.html.twig' %}
{% endblock %}

{%  block titreRubrique %}
	{{ utilisateur.nom }}>{{ entreprise.nom }}><strong>{{ pageName }}</strong>
{% endblock %}

{% block descriptionRubrique %}
	{{ "feedback_description" | trans }}
{% endblock %}

{% block menuLateraleGauche %}
	{% include 'segments/menu_entreprise_dashbord.html.twig' %}
{% endblock %}

{% block body %}
	<div>
		<div class="d-flex shadow-sm sensible bg-white">
			<table class="table table-hover table-sm caption-top p-3">
				<caption>
                    {% if feedbacks|length == 0 %}
                        {{ "list_filter_result_empty" | trans}}
                    {% else %}
                        {{ "list_filter_result_elements" | trans({
                            ':number': feedbacks|length
                        })}}
                    {% endif %}
                    <div class="d-flex gap-1">
                        {{ knp_pagination_render(feedbacks) }}
                    </div>
                </caption>
                {% if feedbacks|length != 0 %}
                    <thead class="table-light">
						<tr>
							<th scope="col">
								<div class='d-flex justify-content-start text-secondary'>
									{{ "list_of_object" | trans }}
								</div>
							</th>
							<th scope="col" style="width:100px" >
								<div class='d-flex justify-content-end'>
									{# {{ "note_list_actions" | trans }} #}
								</div>
							</th>
						</tr>
					</thead>
                    <tbody class="table-group-divider">
                        {# Boucler les donnees #}
                        {% for feedback in feedbacks %}
                            <tr id="row_feedback_{{ feedback.id }}" class="element-a-encadrer parent-a-options">
                                <td>
                                    <a class="m-2 fw-bold" href="{{ path('admin.feedback.edit', {'idFeedback':feedback.id, 'idEntreprise': entreprise.id})}}">
                                        {{ include("segments/icones/feedback.html.twig", {size:'18px'}) }}
                                        "{{ feedback.description }}"
                                    </a><br/>
                                    {% if feedback.tache != null %}
                                        <small class="m-2">
                                            <span class="text-secondary fw-bold">
                                                {{ include("segments/icones/tache.html.twig", {size:'16px'}) }}
                                                "{{ feedback.tache.description }}"
                                            </span> 
                                            {% set couleur = "text-success" %}
                                            {% if constante.Tache_getExecutionStatus(feedback.tache)['remaining days'] < 0 %}
                                                {% set couleur = "text-danger" %}
                                            {% endif %}
                                            <span class="{{ couleur }}">
                                                • {{ constante.Tache_getExecutionStatus(feedback.tache)['text'] }}
                                            </span> 
                                        </small><br/>
                                    {% endif %}
                                    {% if feedback.tache.piste != null %}
                                        <small class="m-2">
                                            <span class="text-secondary fw-bold">
                                                {{ include("segments/icones/piste.html.twig", {size:'16px'}) }}
                                                {{ feedback.tache.piste }}
                                            </span> 
                                        </small><br/>
                                    {% endif %}
                                    {% if feedback.tache.cotation != null %}
                                        <small class="m-2">
                                            <span class="text-secondary">
                                                {{ include("segments/icones/cotation.html.twig", {size:'16px'}) }}
                                                {{ feedback.tache.cotation }}
                                            </span> 
                                        </small><br/>
                                    {% endif %}
                                    {% if feedback.tache.notificationSinistre != null %}
                                        <small class="m-2">
                                            <span class="text-secondary">
                                                {{ include("segments/icones/notificationsinistre.html.twig", {size:'16px'}) }}
                                                {{ feedback.tache.notificationSinistre }}
                                            </span> 
                                        </small><br/>
                                    {% endif %}
                                    {% if feedback.tache.offreIndemnisationSinistre != null %}
                                        <small class="m-2">
                                            <span class="text-secondary">
                                                {{ include("segments/icones/offreindemnisation.html.twig", {size:'16px'}) }}
                                                {{ feedback.tache.offreIndemnisationSinistre }}
                                            </span> 
                                        </small><br/>
                                    {% endif %}
                                    <small class="m-2">
                                        <span class="text-secondary">
                                            {{ include("segments/icones/datetime.html.twig", {size:'18px'}) }}
                                            {{ feedback.createdAt|format_datetime('short', 'short', locale: utilisateur.locale) }}
                                        </span> 
                                        <span class="text-secondary"> • Modifiée le {{ feedback.updatedAt|format_datetime('short', 'short', locale: utilisateur.locale) }}</span> 
                                        {% if feedback.tache != null %}
                                            <span class="text-secondary">
                                                 • {{ include("segments/icones/invite.html.twig", {size:'10px'}) }}
                                                {{ feedback.tache.executor }}
                                            </span> 
                                        {% endif %}
                                    </small><br/>
                                </td>
                                <td>
                                    <div class="options-du-parent">
                                        <div class="d-flex gap-1 d-flex justify-content-end">
                                            {# Les actions sur l element de la liste #}
                                            <a class= "btn btn-outline-primary btn-sm border-0" href="{{ path('admin.feedback.edit', {'idEntreprise': entreprise.id, 'idFeedback':feedback.id })}}" class="" mthod = 'POST'>
                                                {{ include('segments/icones/actions/edit.html.twig', {size:'18px'}) }}
												{# Modifier #}
                                            </a>
                                            <form action="{{ path('admin.feedback.remove', {'idFeedback':feedback.id, 'idEntreprise': entreprise.id})}}" method='POST'>
                                                <input type='hidden' name='_method' value='DELETE'>
                                                <button class='btn btn-outline-danger border-0 btn-sm' type='submit'>
                                                    {{ include('segments/icones/actions/delete.html.twig', {size:'18px'}) }}
													{# Supprimer #}
                                                </button>
                                            </form>
                                        </div>
                                     </div>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                {% endif %}
            </table>
        </div>
    </div>
{% endblock %}
