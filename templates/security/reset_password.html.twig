{% extends 'base.html.twig' %}

{% block title %}{{ pageName|default('Réinitialisation du mot de passe') }} — JS Brokers{% endblock %}

{% block stylesheets %}
{{ parent() }}
{{ include('security/_auth_styles.html.twig') }}
{% endblock %}

{% macro eye_toggle() %}
    {# Bouton afficher/masquer, identique à la page de connexion. #}
    <button type="button" class="auth__toggle"
            data-action="password-toggle#toggle"
            data-password-toggle-target="button"
            aria-pressed="false"
            aria-label="Afficher le mot de passe">
        {# Œil ouvert (état : mot de passe masqué) #}
        <svg data-password-icon viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="2" stroke-linecap="round"
             stroke-linejoin="round" aria-hidden="true">
            <path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7Z"/>
            <circle cx="12" cy="12" r="3"/>
        </svg>
        {# Œil barré (état : mot de passe affiché) #}
        <svg data-password-icon class="is-hidden" viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="2" stroke-linecap="round"
             stroke-linejoin="round" aria-hidden="true">
            <path d="M9.88 9.88a3 3 0 0 0 4.24 4.24"/>
            <path d="M10.73 5.08A10.43 10.43 0 0 1 12 5c6.5 0 10 7 10 7a13.16 13.16 0 0 1-1.67 2.68"/>
            <path d="M6.61 6.61A13.526 13.526 0 0 0 2 12s3.5 7 10 7a9.74 9.74 0 0 0 5.39-1.61"/>
            <line x1="2" y1="2" x2="22" y2="22"/>
        </svg>
    </button>
{% endmacro %}

{% block body %}
{% import _self as f %}
<main class="auth" data-controller="top-progress">

    {# Barre de progression globale, déclenchée à l'envoi du formulaire. #}
    <div class="ent-progress-bar" data-top-progress-target="bar"
         role="progressbar" aria-label="Chargement en cours" aria-hidden="true"></div>

    <div class="auth__card">

        {# ---------- Panneau de marque ---------- #}
        {{ include('security/_auth_brand.html.twig') }}

        {# ---------- Panneau formulaire ---------- #}
        <section class="auth__form">

            {{ include('components/_flash_toasts.html.twig') }}

            <h1 class="auth__title">Nouveau mot de passe</h1>
            <p class="auth__subtitle">
                Choisissez un nouveau mot de passe pour sécuriser votre compte JS Brokers.
            </p>

            {{ form_start(resetForm, {attr: {
                novalidate: 'novalidate',
                'data-controller': 'form-submit',
                'data-action': 'submit->form-submit#submitting submit->top-progress#show',
                'data-form-submit-loading-text-value': 'Réinitialisation…'
            }}) }}

                {# Nouveau mot de passe #}
                {{ form_label(resetForm.plainPassword.first) }}
                <div class="auth__field" data-controller="password-toggle">
                    <svg class="auth__field-icon" viewBox="0 0 24 24" fill="none"
                         stroke="currentColor" stroke-width="2" stroke-linecap="round"
                         stroke-linejoin="round" aria-hidden="true">
                        <rect x="3" y="11" width="18" height="11" rx="2"/>
                        <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                    </svg>
                    {{ form_widget(resetForm.plainPassword.first) }}
                    {{ f.eye_toggle() }}
                </div>
                {{ form_errors(resetForm.plainPassword.first) }}

                {# Confirmation #}
                {{ form_label(resetForm.plainPassword.second) }}
                <div class="auth__field" data-controller="password-toggle">
                    <svg class="auth__field-icon" viewBox="0 0 24 24" fill="none"
                         stroke="currentColor" stroke-width="2" stroke-linecap="round"
                         stroke-linejoin="round" aria-hidden="true">
                        <rect x="3" y="11" width="18" height="11" rx="2"/>
                        <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                    </svg>
                    {{ form_widget(resetForm.plainPassword.second) }}
                    {{ f.eye_toggle() }}
                </div>
                {{ form_errors(resetForm.plainPassword.second) }}

                <button type="submit" class="btn btn-cobalt w-100 auth__submit d-inline-flex align-items-center justify-content-center"
                        data-form-submit-target="button">
                    {{ ux_icon(resolve_icon_name('action:password'), {height: '20px', width: '20px'}) }}
                    <span class="ms-2">Réinitialiser le mot de passe</span>
                </button>

            {{ form_end(resetForm) }}

            <p class="auth__foot">
                <a href="{{ path('app_login') }}" class="auth__foot-link"
                   data-action="click->top-progress#show">Retour à la connexion</a>
            </p>

        </section>
    </div>
</main>
{% endblock %}
