{# Bascule de langue (FR/EN) de la Console : composant unifié.
   On reste sur la page courante en ne changeant que ?lang= (persisté côté
   serveur via applyLangPreference). Voir components/_lang_switch.html.twig. #}
{{ include('components/_lang_switch.html.twig') }}
